# Habilita as bibliotecas modernas do Android (Obrigatório para o Jetpack Compose)
android.useAndroidX=true

# Garante que bibliotecas antigas funcionem com o AndroidX
android.enableJetifier=true

# Dá mais memória para o robô compilar mais rápido
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
